server:
  port: 8080

spring:
  application:
    name: knowledge-base
  
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/test_knowledge_base_1757054758?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false&allowMultiQueries=true
    username: root
    password: xmc131455
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0
  mapper-locations: classpath*:/mapper/**/*.xml

# JWT配置
jwt:
  secret: knowledge-base-secret-key-2024-must-be-very-long-and-secure
  expiration: 604800  # 7天，单位：秒

# Python服务配置
python:
  service:
    url: http://localhost:8000

# 日志配置
logging:
  level:
    com.knowledge: debug
    org.springframework.security: debug

# Swagger配置 (2.9.2版本不需要特殊配置) 